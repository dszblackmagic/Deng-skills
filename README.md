# Deng-Skills

这是一个用于长期维护个人自定义 skills 的仓库。

## 目录结构

```text
skills/     正式维护的 skill 目录，每个 skill 对应一个独立目录
templates/  仓库级模板或起始材料
tools/      仓库级辅助脚本
```

## 使用方式

平时直接浏览 `skills/` 目录即可查找对应的 skill。

每个 skill 都以独立目录的方式存放，便于单独复制、迁移和维护。
